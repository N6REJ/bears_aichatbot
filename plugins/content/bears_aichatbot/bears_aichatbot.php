<?php
/**
 * Entry file for plg_content_bears_aichatbot. Bootstrap is handled via services/provider and namespaced classes.
 */
\defined('_JEXEC') or die;

// No bootstrap logic required; plugin is wired via services/provider
